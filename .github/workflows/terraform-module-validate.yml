name: Terraform Module Validate

on:
  push:
    branches: [main, master]
    paths:
      - '**.tf'
      - '**.tfvars'
      - 'imports.tf'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  validate-migration:
    name: Validate Terraform
    runs-on: ubuntu-latest
    env:
      TF_IN_AUTOMATION: "true"
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID || '' }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET || '' }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID || '' }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID || '' }}
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS || '' }}
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID || '' }}
      ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET || '' }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID || '' }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID || '' }}
    defaults:
      run:
        shell: bash
        working-directory: .
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Azure Login (optional)
        if: ${{ env.AZURE_CREDENTIALS != '' }}
        uses: azure/login@v2
        with:
          creds: ${{ env.AZURE_CREDENTIALS }}

      - name: Terraform Init
        run: terraform init -input=false

      # Terraform Validate (disabled temporarily — re-enable when ready)
      # - name: Terraform Validate
      #   run: terraform validate -no-color

      - name: Detect tfvars file
        id: tfvars
        run: |
          if [ -f "dev.terraform.tfvars" ]; then
            echo "arg=-var-file=dev.terraform.tfvars" >> "$GITHUB_OUTPUT"
            echo "Using dev.terraform.tfvars for plan." >> "$GITHUB_STEP_SUMMARY"
          elif [ -f "terraform.tfvars" ]; then
            echo "arg=-var-file=terraform.tfvars" >> "$GITHUB_OUTPUT"
            echo "Using terraform.tfvars for plan." >> "$GITHUB_STEP_SUMMARY"
          elif ls ./*.auto.tfvars >/dev/null 2>&1; then
            auto_file="$(ls ./*.auto.tfvars | head -n 1)"
            echo "arg=-var-file=${auto_file}" >> "$GITHUB_OUTPUT"
            echo "Using ${auto_file} for plan." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "arg=" >> "$GITHUB_OUTPUT"
            echo "⚠️ No .tfvars file detected. Plan may fail if variables have no defaults." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Terraform Plan
        id: plan
        run: |
          set +e
          terraform plan -input=false ${{ steps.tfvars.outputs.arg }} -out=tfplan -detailed-exitcode
          exit_code=$?
          set -e
          if [ "$exit_code" -eq 1 ]; then
            echo "Terraform plan failed"
            exit 1
          fi
          echo "exit_code=$exit_code" >> "$GITHUB_OUTPUT"

      - name: Export plan summary
        run: |
          terraform show -json tfplan > tfplan.json
          jq '{
            add: ([.resource_changes[]? | select(.change.actions | index("create"))] | length),
            change: ([.resource_changes[]? | select(.change.actions | index("update"))] | length),
            destroy: ([.resource_changes[]? | select(.change.actions | index("delete"))] | length)
          }' tfplan.json > plan-summary.json
          echo "Plan summary:" >> "$GITHUB_STEP_SUMMARY"
          cat plan-summary.json >> "$GITHUB_STEP_SUMMARY"

      - name: Upload tfplan artifact
        uses: actions/upload-artifact@v4
        with:
          name: terraform-module-plan-artifacts
          path: |
            ./tfplan
            ./tfplan.json
            ./plan-summary.json
          if-no-files-found: error

      - name: Summary
        run: |
          if [ "${{ steps.plan.outputs.exit_code }}" = "0" ]; then
            echo "No infrastructure drift detected." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Terraform plan completed with pending changes." >> "$GITHUB_STEP_SUMMARY"
          fi
